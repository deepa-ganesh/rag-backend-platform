<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">
    <!-- Enable Spring Boot profile support -->
    <springProperty name="APP_NAME" source="spring.application.name" defaultValue="app" />
    <springProperty name="LOGSTASH_HOST" source="LOGSTASH_HOST" defaultValue="localhost"/>
    <springProperty name="LOGSTASH_PORT" source="LOGSTASH_PORT" defaultValue="5001"/>

    <!-- Default Spring Boot setup -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <!-- Non-local environment logging -->
    <springProfile name="dev, prod">
        <appender name="LOGSTASH" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
            <destination>${LOGSTASH_HOST}:${LOGSTASH_PORT}</destination> <!-- Logstash port -->
            <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
                <providers>
                    <timestamp>
                        <fieldName>timestamp</fieldName>
                    </timestamp>
                    <pattern>
                        <pattern>
                            {
                            "timestamp": "%d{yyyy-MM-dd'T'HH:mm:ss.SSSZ}",
                            "level": "%level",
                            "app": "${APP_NAME}",
                            "trace.id": "%X{traceId:-}",
                            "span.id": "%X{spanId:-}",
                            "thread": "%thread",
                            "logger": "%logger",
                            "message": "%message"
                            }
                        </pattern>
                    </pattern>
                    <stackTrace />
                </providers>
                <immediateFlush>true</immediateFlush>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="LOGSTASH" />
        </root>
    </springProfile>

    <springProfile name="local">
        <!-- Console Appender -->
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>
                    %d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n
                </pattern>
                <immediateFlush>true</immediateFlush>
            </encoder>
        </appender>

        <!-- Logstash Appender (JSON logs for ELK stack) -->
        <appender name="LOGSTASH" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
            <destination>${LOGSTASH_HOST}:${LOGSTASH_PORT}</destination>
            <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
                <providers>
                    <timestamp>
                        <fieldName>timestamp</fieldName>
                    </timestamp>
                    <pattern>
                        <pattern>
                            {
                            "timestamp": "%d{yyyy-MM-dd'T'HH:mm:ss.SSSZ}",
                            "level": "%level",
                            "app": "${APP_NAME}",
                            "trace.id": "%X{traceId:-}",
                            "span.id": "%X{spanId:-}",
                            "thread": "%thread",
                            "logger": "%logger",
                            "message": "%message"
                            }
                        </pattern>
                    </pattern>
                    <stackTrace />
                </providers>
            </encoder>
        </appender>

        <!-- Root Logger with both -->
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
            <appender-ref ref="LOGSTASH" />
        </root>
    </springProfile>

</configuration>