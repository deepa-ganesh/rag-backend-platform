server:
  port: 8081
  servlet:
    context-path: /ragchatstorage

spring:
  application:
    name: chatstorage-service

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: always